import React  from 'react';
import "./style.css"

const TheFooter = () =>{

    return (
            <div className="footer">
                <h4>Footer</h4>
            </div>
        );
}

export default React.memo(TheFooter);
